akka {
  event-handlers = ["akka.event.slf4j.Slf4jEventHandler"]
  log-level = DEBUG
}

app {
	port = 3000
	interface = "localhost"
}

prod_db = {
  dataSourceClass = "org.postgresql.ds.PGSimpleDataSource"
  properties = {
    databaseName = "bilby_io"
    user = "play"
    password = "play"
  }
  numThreads = 8
}