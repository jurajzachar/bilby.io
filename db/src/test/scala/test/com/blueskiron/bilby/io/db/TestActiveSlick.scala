package test.com.blueskiron.bilby.io.db
import org.scalatest.FunSuite

class TestActiveSlick extends FunSuite{
 
   test("Entity type is captured for User") {
   
//     val usersRepo = ActiveSlickWithCodegen.UsersRepo
//     usersRepo.save(entity)
   }
  
}